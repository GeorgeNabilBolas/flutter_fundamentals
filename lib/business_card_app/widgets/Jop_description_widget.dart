import 'package:flutter/material.dart';

class JopDescriptionWidget extends StatelessWidget {
  const JopDescriptionWidget({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text(
      'FLUTTER DEVELOPER',
      style: TextStyle(
        fontSize: 16,
        color: Colors.black87,
      ),
    );
  }
}
